[Unit]
Description=Ghosted Next.js Web App
After=network.target

[Service]
Type=simple
User=ghosted
Group=ghosted
WorkingDirectory=/opt/ghosted/current-web
EnvironmentFile=/etc/ghosted/ghosted.env
ExecStart=/usr/bin/node /opt/ghosted/current-web/server.js
Restart=always
RestartSec=5

# Keep runtime state outside the release bundle.
ReadWritePaths=/var/lib/ghosted

[Install]
WantedBy=multi-user.target
