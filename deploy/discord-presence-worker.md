# Discord Presence Worker Setup

The canonical guide moved to [discord-worker.md](./discord-worker.md).

This file remains as a compatibility pointer because the runtime is now a generalized Discord worker host with a `voicePresence` module, not a single-purpose presence daemon.
